import { Icon } from "./icon";

/**
 * Storybook Definition.
 */

const meta = {
	title: "Atoms/Icon",
	component: Icon
}

export default meta;

export const Default = {
	args: {
		label: "Icon",
	},
};

import { Input } from "./input";

/**
 * Storybook Definition.
 */

const meta = {
    title: 'Atoms/Input',
    component: Input
}

export default meta;

export const Default = {
    args: {
        label: "Input",
    },
};

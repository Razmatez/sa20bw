import { Share } from "./share";

/**
 * Storybook Definition.
 */

const meta = {
	title: 'Atoms/Share',
	component: Share
}

export default meta;

export const Default = {
	args: {
		label: "Share",
	},
};
